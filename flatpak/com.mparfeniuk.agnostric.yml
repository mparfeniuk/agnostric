app-id: com.mparfeniuk.agnostric
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: '25.08'
sdk-extensions:
  - org.freedesktop.Sdk.Extension.node24
command: agnostric
separate-locales: false
finish-args:
  - --share=ipc
  - --device=dri
  - --socket=fallback-x11
  - --socket=wayland
  - --socket=pulseaudio
  - --share=network
  - --env=ELECTRON_TRASH=gio
  - --env=XCURSOR_PATH=/run/host/user-share/icons:/run/host/share/icons
build-options:
  append-path: /usr/lib/sdk/node24/bin
modules:
  - name: agnostric
    buildsystem: simple
    build-options:
      env:
        XDG_CACHE_HOME: /run/build/agnostric/flatpak-node/cache
        npm_config_cache: /run/build/agnostric/flatpak-node/npm-cache
        npm_config_nodedir: /usr/lib/sdk/node24
        npm_config_offline: 'true'
    sources:
      - generated-sources.json
      - type: dir
        path: ..
        dest: main
        skip:
          - .git
          - .github
          - .claude
          - node_modules
          - dist
          - dist-electron
          - dev-dist
          - release
          - flatpak/.flatpak-builder
          - flatpak/build-dir
          - flatpak/repo
      - type: file
        path: run.sh
      - type: file
        path: com.mparfeniuk.agnostric.desktop
      - type: file
        path: com.mparfeniuk.agnostric.metainfo.xml
      - type: file
        path: ../public/favicon/pwa-512x512.png
      - type: file
        path: ../public/favicon/pwa-192x192.png
    build-commands:
      - cd main && npm ci --offline
      - |
        . flatpak-node/electron-builder-arch-args.sh
        cd main
        npm run electron:build -- $ELECTRON_BUILDER_ARCH_ARGS --linux --dir --publish never
      - install -d /app/agnostric
      - cp -a main/release/*/linux*unpacked/. /app/agnostric/
      - if [ -x /app/agnostric/Agnostric ] && [ ! -e /app/agnostric/agnostric ]; then ln -s Agnostric /app/agnostric/agnostric; fi
      - install -Dm755 run.sh /app/bin/agnostric
      - install -Dm644 com.mparfeniuk.agnostric.desktop /app/share/applications/com.mparfeniuk.agnostric.desktop
      - install -Dm644 com.mparfeniuk.agnostric.metainfo.xml /app/share/metainfo/com.mparfeniuk.agnostric.metainfo.xml
      - install -Dm644 pwa-512x512.png /app/share/icons/hicolor/512x512/apps/com.mparfeniuk.agnostric.png
      - install -Dm644 pwa-192x192.png /app/share/icons/hicolor/192x192/apps/com.mparfeniuk.agnostric.png